query PairsDailyVolume($timestampLow: Int, $timestampHigh: Int, $pairs: [String!]) {
  pairDaySnapshots(first: 1000, where: { date_gt: $timestampLow, date_lt: $timestampHigh, pair_in: $pairs }) {
    id
    volumeUSD
    feesUSD
  }
}
